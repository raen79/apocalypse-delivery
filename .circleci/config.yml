version: 2

jobs:
  test:
    docker:
      - image: circleci/ruby:2.7
    steps:
      - checkout
      - run: bundle install
      - run: bundle exec rspec
      - run: bundle exec rubocop
      - run: rails format:check

workflows:
  version: 2
  build:
    jobs:
      - test
